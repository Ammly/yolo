apiVersion: v1
kind: Service
metadata:
   name: yolomy_client
   labels:
     run: yolomy_client
spec:
   ports:
   – port: 80
     targetPort: 8888
     protocol: TCP
   type: NodePort
   selector:
     run: yolomy_client

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
   name: yolomy_client
spec:
   replicas: 2
   template:
     metadata:
       labels:
         run: yolomy_client
   spec:
     containers:
     – name: yolomy_client
       image: ammlyf/yolo_client
       ports:
       – containerPort: 8888
